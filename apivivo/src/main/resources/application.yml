spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:53306}/${DB_NAME:vivo}
    username: wizardry
    password: maosMacias2@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
