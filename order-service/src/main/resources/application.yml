server:
  port: 3000

spring:
  kafka:
    bootstrap-servers: ${KAFKA_BROKER:localhost:9092}
    topic:
      start-saga: start-saga
      notify-ending: notify-ending
    consumer:
      group-id: order-group
      auto-offset-reset: latest

 data:
    mongodb:
      uri: mongodb://order:order@mongodb:27011/order-db

logging:
  level:
    org:
      apache:
        kafka: ON